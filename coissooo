#include <stdio.h>
#include <string.h>

int main(int argc, char const *argv[])
{
	char str[100],mychr;
	int i,numberofstages,stagesofrocket;
	scanf("%d%d",&numberofstages,&stagesofrocket);
	scanf(" %[^\n]s",str);
	//puts(str);
	int m;
	m=strlen(str);
	int letters[m],numofletters[m];
	for(i=0;i < m;i++)
	{
		letters[i]=str[i];
		//printf("%d\n",letters[i]);
	}
	for(i=0;i < m;i++)
	{
		if(letters[i] < 121 && (letters[i+1] == (letters[i]+2))) 
		{
			numofletters[]
		}
		else if(letters[i] == 121 && letters[i] == 97)
		{

		}
		else if(letters[i] == 122 && letters[i] == 98)
		{

		}
	}
	
	return 0;
}
